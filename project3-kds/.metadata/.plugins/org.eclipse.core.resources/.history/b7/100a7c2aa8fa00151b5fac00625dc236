#include "MKL25Z4.h"

static int i = 0;

int main(void)
{
    /* Never leave main */
    return 0;
}

char * itoa(uint8_t * buffer, int32_t num, uint32_t base){
	if(!buffer || base < 0)
		return 0;

}
